import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'es.cristiancode2023.listadelacompra',
  appName: 'La lista de la compra',
  webDir: 'www',
  server: {
    androidScheme: 'https',
  },
};

export default config;
